{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "allowJs": true
  },
  "include": ["**/*.ts"]
  // Do not add a "references" here pointing to our code.
  // The integration tests are supposed to know nothing about
  // our actual implementation.
}
